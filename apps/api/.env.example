# API Port
PORT=3001

# CORS Origin (optional, defaults to allow all)
# For production, set to your frontend URL: https://yourapp.vercel.app
# CORS_ORIGIN=https://yourapp.vercel.app

# Master Key (32 bytes = 64 hex characters)
# If not provided, a key will be auto-generated and saved to data/.master.key
# IMPORTANT: Keep this key backed up! Without it, you cannot decrypt existing data.
# Generate a new key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# MASTER_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Custom path for master key file (optional)
# MASTER_KEY_FILE=./data/.master.key
